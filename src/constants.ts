/** Kittl auth provider key matching the `oauthProviders` entry in manifest.json. */
export const DBX_PROVIDER = 'dropbox';

/** localStorage key for persisting the "Hide folders" mode selection. */
export const FILES_ONLY_KEY = 'dropbox_files_only';

/** Number of file cards to render per scroll page. */
export const PAGE_SIZE = 40;
